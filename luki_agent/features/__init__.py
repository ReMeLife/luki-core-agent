"""Feature-level configuration and helpers for LUKi core agent.

Currently includes token-tier feature maps and helpers for wallet-aware
experiences. This module is intentionally light-weight and contains no
external dependencies, so it can be safely imported from anywhere in
`luki_agent`.
"""
